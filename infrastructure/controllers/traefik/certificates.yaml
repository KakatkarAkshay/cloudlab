apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-kakatkarakshay-dev
  namespace: traefik
spec:
  secretName: traefik-kakatkarakshay-dev-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  dnsNames:
    - traefik.kakatkarakshay.dev
