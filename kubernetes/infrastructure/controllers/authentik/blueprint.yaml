apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprints
  namespace: authentik
data:
  cloudlab-forward-auth.yaml: |
    version: 1
    metadata:
      name: cloudlab-forward-auth
    entries:
      - model: authentik_sources_oauth.oauthsource
        id: google
        identifiers:
          slug: google
        attrs:
          name: Google
          enabled: true
          provider_type: google
          consumer_key: !Env GOOGLE_CLIENT_ID
          consumer_secret: !Env GOOGLE_CLIENT_SECRET
          # Enrollment is deliberately unset: only existing users may link.
          user_matching_mode: email_link
          authentication_flow:
            !Find [authentik_flows.flow, [slug, default-source-authentication]]
      - model: authentik_core.group
        id: admin
        identifiers:
          name: admin
        attrs:
          is_superuser: true
      - model: authentik_core.group
        id: member
        identifiers:
          name: member
      - model: authentik_core.group
        id: guest
        identifiers:
          name: guest
      - model: authentik_core.user
        identifiers:
          username: akshay
        attrs:
          name: Akshay Kakatkar
          email: !Env ROOT_EMAIL
          is_active: true
          groups:
            - !Find [authentik_core.group, [name, authentik Admins]]
            - !KeyOf admin
      - model: authentik_stages_identification.identificationstage
        identifiers:
          name: default-authentication-identification
        attrs:
          sources:
            - !KeyOf google

      - model: authentik_providers_proxy.proxyprovider
        id: headlamp
        identifiers:
          name: headlamp
        attrs:
          mode: forward_single
          external_host: https://headlamp.kakatkarakshay.dev
          authorization_flow:
            !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow:
            !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      - model: authentik_core.application
        identifiers:
          slug: headlamp
        attrs:
          name: Headlamp
          provider: !KeyOf headlamp
          meta_description: Kubernetes cluster UI
          meta_launch_url: https://headlamp.kakatkarakshay.dev/
          meta_icon: https://cdn.jsdelivr.net/gh/selfhst/icons/svg/headlamp.svg

      - model: authentik_providers_proxy.proxyprovider
        id: longhorn
        identifiers:
          name: longhorn
        attrs:
          mode: forward_single
          external_host: https://longhorn.kakatkarakshay.dev
          authorization_flow:
            !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow:
            !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      - model: authentik_core.application
        identifiers:
          slug: longhorn
        attrs:
          name: Longhorn
          provider: !KeyOf longhorn
          meta_description: Distributed block storage
          meta_launch_url: https://longhorn.kakatkarakshay.dev/
          meta_icon: https://cdn.jsdelivr.net/gh/selfhst/icons/svg/rancher-longhorn.svg

      - model: authentik_providers_proxy.proxyprovider
        id: codex
        identifiers:
          name: codex
        attrs:
          mode: forward_single
          external_host: https://codex.kakatkarakshay.dev
          authorization_flow:
            !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow:
            !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      - model: authentik_core.application
        identifiers:
          slug: codex
        attrs:
          name: Codex LB
          provider: !KeyOf codex
          meta_description: Codex load balancer
          meta_launch_url: https://codex.kakatkarakshay.dev/
          meta_icon: https://cdn.jsdelivr.net/gh/selfhst/icons/svg/openai.svg

      - model: authentik_providers_proxy.proxyprovider
        id: traefik
        identifiers:
          name: traefik
        attrs:
          mode: forward_single
          external_host: https://traefik.kakatkarakshay.dev
          authorization_flow:
            !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow:
            !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      - model: authentik_core.application
        identifiers:
          slug: traefik
        attrs:
          name: Traefik
          provider: !KeyOf traefik
          meta_description: Ingress controller dashboard
          meta_launch_url: https://traefik.kakatkarakshay.dev/dashboard/
          meta_icon: https://cdn.jsdelivr.net/gh/selfhst/icons/svg/traefik.svg

      - model: authentik_providers_oauth2.oauth2provider
        id: pangolin
        identifiers:
          name: pangolin
        attrs:
          client_type: confidential
          # Empty rejects every flow; authentik ignores this on update.
          grant_types:
            - authorization_code
            - refresh_token
          sub_mode: user_email
          redirect_uris:
            - matching_mode: strict
              url: https://pangolin.kakatkarakshay.dev/auth/idp/1/oidc/callback
          authorization_flow:
            !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow:
            !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
      - model: authentik_core.application
        identifiers:
          slug: pangolin
        attrs:
          name: Pangolin
          provider: !KeyOf pangolin
          meta_description: Tunnelled reverse proxy
          meta_launch_url: https://pangolin.kakatkarakshay.dev/
          meta_icon: https://cdn.jsdelivr.net/gh/selfhst/icons/svg/pangolin.svg

      - model: authentik_outposts.outpost
        identifiers:
          managed: goauthentik.io/outposts/embedded
        attrs:
          providers:
            - !KeyOf headlamp
            - !KeyOf longhorn
            - !KeyOf codex
            - !KeyOf traefik
          config:
            authentik_host: https://auth.kakatkarakshay.dev
            authentik_host_browser: https://auth.kakatkarakshay.dev
            authentik_host_insecure: false
            log_level: info
            object_naming_template: ak-outpost-%(name)s
            refresh_interval: minutes=5

      # Superseded by the per-application providers above.
      - model: authentik_core.application
        state: absent
        identifiers:
          slug: cloudlab
      - model: authentik_providers_proxy.proxyprovider
        state: absent
        identifiers:
          name: cloudlab-forward-auth
