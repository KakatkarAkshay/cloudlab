apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cloudlab-kakatkarakshay-dev
  namespace: traefik
spec:
  secretName: cloudlab-kakatkarakshay-dev-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  dnsNames:
    - cloudlab.kakatkarakshay.dev
